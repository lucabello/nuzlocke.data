{
  "name": "nuzlocke-data",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "generate": "node src/processors/parsePatch.js && node src/processors/parseLeague.js",
    "validate": "just validate"
  }
}
